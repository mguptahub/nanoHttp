name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build AMD64 binary
        env:
          GOOS: linux
          GOARCH: amd64
        run: |
          go build -o justHttp-linux-amd64 cmd/justhttp/main.go
          chmod +x justHttp-linux-amd64

      - name: Build ARM64 binary
        env:
          GOOS: linux
          GOARCH: arm64
        run: |
          go build -o justHttp-linux-arm64 cmd/justhttp/main.go
          chmod +x justHttp-linux-arm64

      - name: Build macOS AMD64 binary
        env:
          GOOS: darwin
          GOARCH: amd64
        run: |
          go build -o justHttp-darwin-amd64 cmd/justhttp/main.go
          chmod +x justHttp-darwin-amd64

      - name: Build macOS ARM64 binary
        env:
          GOOS: darwin
          GOARCH: arm64
        run: |
          go build -o justHttp-darwin-arm64 cmd/justhttp/main.go
          chmod +x justHttp-darwin-arm64

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            justHttp-linux-amd64
            justHttp-linux-arm64
            justHttp-darwin-amd64
            justHttp-darwin-arm64
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 